#include <vector>

struct cell
{
    int x;
    int y;
    bool alive;
};

int getNumNeighbors()

int main()
{
    int cols;
    int lines;
    int turns;

    string tile;
    fin >> cols >> lines >> turns;
    std::vector<cell> grid;
 for(int y = 0; y < cols; y++)
 {
     for (int x = 0; x < lines; x++)
     {
         if()
         grid.push_back(new cell(x, y,))
     }
 }
 for(int i = 0; i < turns; i++)
 {
     for(int y = 0; y < cols; y++)
     {
         for (int x = 0; x < lines; x++)
         {

         }
     }
 }
  return 0;
};