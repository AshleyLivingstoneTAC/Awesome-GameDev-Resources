int main()
{
    int cols;
    int lines;
    int turns;
    bool grid[lines][cols];
    string tile;
    fin >> cols >> lines >> turns;
 for(int y = 0; y < cols; y++)
 {
     for (int x = 0; x < lines; x++)
     {
         if (tile == ".")
         {
             grid[x][y] = false;
         }
         if (tile == "#")
         {
             grid[x][y] = true;
         }
     }
 }
 for(int i = 0; i < turns < i++)
 {
     for(int y = 0; y < cols; y++)
     {
         for (int x = 0; x < lines; x++)
         {

         }
     }
 }
  return 0;
};